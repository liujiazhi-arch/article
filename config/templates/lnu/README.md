此目录用于存放辽宁大学官方模板组件和固定封面资产。

从官方模板 `.docx` 解压后手动放入。工具在修复时若此目录有对应文件，将自动注入用户文档。

`cover-assets/wordmark.jpeg` 和 `cover-assets/emblem.png` 从辽宁大学官方封面模板提取。提取时去除了示例教学文本框、删除提示和箭头，只保留校名字样与校徽，不包含学生身份信息。

固定封面只有在用户显式选择 `cover`、六项字段完整且封面边界可确认时才会生成。输出仍需在实际提交环境的 Word/WPS 中人工复核。
