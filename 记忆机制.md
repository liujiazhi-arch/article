# 论文格式检查工具记忆机制

## 目的

这套记忆机制用于解决同一个问题：

- 下一次继续改项目时，能快速知道已经改到什么程度
- 哪些规则已经主链化
- 哪些只是专项经验
- 新论文再出现同类问题时，能判断这是工具缺陷、规则缺口，还是文档脏数据个例

原则：

- 记忆文件负责“持续理解项目状态”
- `Git` 负责“保存文件历史”
- 记忆文件只保存当前事实、稳定原则、已知边界和下一步方向
- 逐轮流水、对话过程和旧测试数字交给 `Git` 历史，不再写进长期记忆入口

## 文件分层

### 1. 项目总账

文件：

- `项目修改日志.md`

作用：

- 记录项目级里程碑
- 记录已经进入主链的规则
- 记录当前已自动化边界与人工复核边界
- 记录最近一次重要验证结果
- 记录下一步建议

使用规则：

- 只有“大型项目级修改”才更新
- 不记录每一篇论文的全部细节
- 必须是当前项目的唯一总入口

### 2. 当前交接状态

文件：

- `当前交接状态.md`

作用：

- 给下一次接手的 Codex 快速读
- 用最短内容说明：
  - 当前主线
  - 最近完成
  - 最新产物
  - 未完成
  - 下次先看哪些文件

使用规则：

- 每轮较大修改结束时更新
- 保持短，不写成长文

### 3. 专项经验

文件：

- `references/lnu/lnu-implementation-memory.md`
- `记忆/专项规则/lnu/`

作用：

- 存学校专项、规则专项、事故专项的深度经验
- 记录为什么这样改
- 记录已经踩过的坑
- 记录验证方式和推荐执行方式
- 记录“章节类型 -> 对应规则 -> 当前状态”的专项拆分结果

使用规则：

- 只有当某一专项已经值得复用时才追加
- 不把项目总状态写到专项文件里

### 4. 每日记录

目录：

- `记忆/每日记录/`

作用：

- 按天记录当天做了什么
- 作为“过程记忆”和“当天工作摘要”
- 方便后续回看某天到底做了哪些动作

使用规则：

- 每天最多一份
- 记录当天新增规则、验证文件、真实文档处理结果、暴露出的新问题
- 不替代项目总账

### 5. 单文档输出日志

目录：

- `outputs/`

作用：

- 为某一份论文产物保留本次修改说明

使用规则：

- 视为局部日志，不视为长期项目入口
- 重要经验必须再沉淀回：
  - `项目修改日志.md`
  - 专项 `memory.md`
  - 当日记录
- 现在 `outputs/` 采用三层管理：
  - 根目录工作产物默认不进 Git
  - `outputs/index/` 里的索引和里程碑记录进 Git
  - `outputs/milestones/` 只保留少量关键阶段成果

## 每次收尾时的固定动作

完成一轮项目级修改后，固定做这四件事：

1. 更新 `当前交接状态.md`，只写当前事实和下一步
2. 若主链能力变化，更新 `项目修改日志.md`
3. 若属于专项规则，更新对应 `implementation-memory.md`
4. 必要时在 `记忆/每日记录/` 下追加当天摘要，不写长篇对话过程

若本轮同时改了主链代码、profile、测试或记忆结构，再补第五件事：

5. 提交一次清晰的 Git 记录

## 问题归类规则

以后新论文再出现问题时，统一按下面分类：

### A. 工具问题

满足任一条件时，视为工具问题：

- 该规则已经进入 profile
- 该规则已经进入 `audit` / `fix` / `verify` 主链
- 项目日志和专项记忆中都明确写过“已稳定支持”
- 但新论文放进去后同类问题仍未被正确审出或修好

结论：

- 这是工具缺陷、主链回归或覆盖不足
- 不应再把它当作“人工审查疏漏”

### B. 规则缺口

满足任一条件时，视为规则缺口：

- 学校样本里有明确要求
- 但 profile 或 runtime 里还没有这条规则
- 或者只存在经验记忆，没有真正进主链

结论：

- 需要补 profile / audit / fix / tests

### C. 文档个例

满足任一条件时，先视为文档个例：

- 文档中出现占位符、脏样式、异常结构
- 当前主链并未承诺自动处理这一类问题
- 或者问题高度依赖语义和人工判断

结论：

- 先单独修文档
- 再判断这类问题是否值得沉淀成主链规则

## 当前记忆入口顺序

以后继续本项目时，默认按这个顺序读取：

1. `AGENTS.md`
2. `当前交接状态.md`
3. `项目修改日志.md`
4. 相关专项记忆
5. 当天要处理的具体文档

## Git 与记忆的边界

- 记忆文件负责“下次先看什么、为什么这样改、当前改到哪一步”
- `Git` 负责“这一改动具体改了哪些文件、什么时候改的、能否回退”
- `outputs/` 属于运行产物，不进入版本历史
- `outputs/index/` 和 `outputs/milestones/` 属于例外，是为了让 Git 能看见阶段结果与关键产物
- `项目修改日志.md`、`当前交接状态.md`、`记忆/` 和专项记忆应进入版本历史
- 如果以后出现“记忆文件内容”和代码状态不一致，以代码和最近一次 Git 提交为准，再回补记忆文件

## 记忆机制当前结论

- 图规则主链已经进入稳定阶段
- 表块留白与表题贴表规则已经进入主链
- 图表跨页排布优化已经进入“显式开启、默认不变”的稳定阶段
- 如果新论文出现跨页大空白，优先走 `figures_tables + --layout-rebalance`
- 辽大参考文献版式主链已经进入稳定阶段
- 如果新论文再出现“图块留白/表名离表太远/表后与下文挤在一起/图题图注行距/参考文献 `[01]`/参考文献悬挂缩进错误”等问题，应优先判定为工具问题，而不是重新讨论是否要这样改
