# 当前交接状态

## 当前事实

- 分支：`codex/readiness-scenario-hardening`
- 工作树：上一轮已清干净，本轮只在压缩记忆文件
- 主入口：`scripts/thesis_workbench.py`
- 稳定主链：`thesis_workbench.py -> thesis_tool/workflow.py -> audit_thesis.py / fix_thesis.py -> _thesis_utils.py`
- 当前辽大 profile：只保留 `config/profiles/lnu-checker-2026.yaml`
- 当前辽大入口：`lnu`、`lnu-checker`、`lnu-checker-2026`
- 已移除：`lnu-undergraduate` profile 文件和运行时别名
- 已移除：`ams-graduate.yaml`

## 当前能力

- `cn-common` runtime：49 条规则
- `lnu` runtime：73 条规则
- profile catalog 已暴露三类一等支持场景：
  - `课程作业/基础论文`
  - `普通论文或综述`
  - `学校学位论文`
- scope 已覆盖：
  - `page`
  - `abstract`
  - `toc`
  - `headings`
  - `body_paragraphs`
  - `figures_tables`
  - `references`
  - `acknowledgement`
  - `appendix`

## 最近已完成

- 辽大最新 profile 合并到 `lnu-checker-2026`
- 旧 profile 和旧别名清理完成
- LNU 安全文本规则已接入：
  - 摘要、目录、正文的中英/中数字混排空格紧凑化
  - 数字和单位之间保留空格
  - 正文编号标题如 `1.2 标题` 保留编号后空格
  - 英文摘要/英文关键词保留半角英文标点
- 参考文献 section 停止边界已加固，避免误吞附录/致谢等后续部分
- article 后端/本地壳相关测试已补强
- 项目说明、LNU 历史基线和 AGENTS 指南已纳入 Git

## 最新验证

- 全量测试：`523 passed, 6 skipped, 1 xfailed`
- profile catalog：
  - `cn-common`
  - `lnu-checker-2026`
- `lnu-checker-2026` aliases：
  - `lnu`
  - `lnu-checker`
  - `lnu-checker-2026`

## 当前边界

- 封面不属于当前自动修复主线，默认保留原样
- 自动目录仍依赖 Word/WPS 刷新域，工具负责插入/清理结构
- `--layout-rebalance` 是显式开关，默认不动图表锚点
- 图表紧凑排版仍是启发式，不读取真实页面剩余高度
- 参考文献标题占位符如 `—— -> 参考文献` 尚未完全主链化
- 野生文档识别、自动判别和渲染闭环仍是下一阶段重点

## 下一步优先级

1. 野生文档识别：先判断文档结构、目录来源、标题样式污染、参考文献/附录边界和对象块异常。
2. 渲染闭环：优先围绕自动目录、图表紧凑排布、WPS/Word 视觉复核建立可重复流程。
3. 文档压缩：长期记忆只保留当前事实、稳定原则和已知边界，旧流水账交给 Git 历史。
4. README 拆分：后续可把 article 后端 runbook 从 README 拆到独立文档。

## 下次先读

1. `AGENTS.md`
2. `当前交接状态.md`
3. `项目修改日志.md`
4. `references/lnu/lnu-implementation-memory.md`
5. `config/profiles/lnu-checker-2026.yaml`
