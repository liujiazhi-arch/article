# 当前交接状态

## 当前主线

- 辽宁大学本科论文格式工具
- `article` 后端稳定化补强

## 最近完成

- 图规则主链已收口：
  - `LNU_F03`
  - `LNU_F06`
  - 图块整体归位、图前后留白、图题图注分离修复
- 表块规则已补入主链：
  - `LNU_TB04`
  - 表题贴表
  - 表后与正文/图块留白统一结算
- 已新增可选流程：
  - `--layout-rebalance`
  - 默认关闭
  - 仅在 `figures_tables` 范围内显式开启时执行
  - 当前用于减少图块跨页造成的大面积留白
- 参考文献主链已收口：
  - `[N]` 后直接接正文内容
  - 禁止 `[01]`
  - 悬挂缩进 `420 / -420`
  - 宋体五号 + 英文 `Times New Roman`
  - `1.5` 倍行距
  - 段前后 `0`
- 已建立辽大章节专项拆分：
  - `记忆/专项规则/lnu/00-章节规则总表.md`
  - `01-前置部分.md`
  - `02-正文与对象块.md`
  - `03-后置部分.md`
- 已落地 Git 基础设施：
  - `.gitignore`
  - `.gitattributes`
  - `Git使用规范.md`
  - Git 与记忆机制边界已写回项目文档
- 已落地输出分层纳管：
  - `outputs/index/`
  - `outputs/milestones/`
  - `scripts/output_versioning.py`
- 已继续做 frontmatter / TOC 低风险共享化整理：
  - `scripts/frontmatter_utils.py`
  - `_thesis_utils.py`
  - `audit_thesis.py`
  - `thesis_tool/workflow.py`
  - `fix_thesis.py`
  - 本轮新增“连续目录块范围识别”共享 helper
  - `repair_misplaced_abstract_keywords()` 与 `remove_existing_toc_artifacts()` 已共用目录块边界检测
- 当前测试与烟测基线已刷新：
  - `python3 -m pytest -q` -> `523 passed, 6 skipped, 1 xfailed`
  - article 定向回归 -> `148 passed`
  - FastAPI HTTP smoke -> `6 passed`
  - 真实论文 `audit / verify` -> `100/100`
- `article` 后端本轮继续收口：
  - fake app / FastAPI 下载契约分叉已修平
  - stale `running` recovery 冻结快照测试已补
  - cleanup 后 upload-backed retry 测试已补
  - `wait_for_job(timeout)` 不改持久化状态测试已补
  - jobs runtime 最小 lease / heartbeat 元数据骨架已补并加测试锁定
  - retention automation 最小版已落地：
    - `POST /ops/retention/sweep`
    - job/upload 到期清理复用现有 cleanup 语义
    - cleanup 审计 `policy` 标记为 `retention`
    - `POST /ops/retention/run-defaults`
    - 基于环境变量的机会式 autorun retention 最小版
  - readiness / version / 结构化观测最小版已落地：
    - `GET /ready`
    - `GET /version`
    - `GET /ops/summary`
  - 只读运维检查已继续补强：
    - `GET /ops/storage`
    - `GET /ops/runtime`
    - `GET /ready` 现在同时检查 `runtime_root` 可写
    - `GET /ops/summary` 现在包含 cleanup 计数、storage index 计数与 runtime 摘要
    - storage 现在可返回 `quick_check`、WAL、表行数、db 路径、索引存在性
  - 本地应用壳层已继续推进：
    - `article-local` 统一子命令入口
    - `article-api`
    - `article-doctor`
    - `article-maintain`
    - `article-backup`
    - `article-restore`
    - `ARTICLE_API_RUNTIME_ROOT` 环境变量覆盖已补
    - backup / restore 现在按“状态数据 / 运行产物”分层归档
    - restore 会校验备份 manifest 的 schema 兼容性
    - restore 会拒绝归档成员越界写出恢复根目录
    - storage maintenance 现在支持 `ANALYZE` 和可选 `VACUUM`
    - storage 现在已进入显式 migration chain：`0 -> 1 -> 2`
    - runtime 摘要现在暴露：
      - recovered failed 计数
      - stale heartbeat 观测
      - recovery grace window 观测
      - pending recovery job 计数
    - 相关环境变量：
      - `ARTICLE_API_JOB_HEARTBEAT_STALE_SECONDS`
      - `ARTICLE_API_JOB_RECOVERY_GRACE_SECONDS`
  - recovery/runtime 补强已继续落地：
    - stale recovery 会补齐缺失 runtime 合同字段
    - stale apply 若 frozen result 在而 artifacts 丢失，会按 frozen result 重建最小 artifacts 语义
  - jobs runtime 结构化事件流已落地：
    - `job_queued`
    - `worker_started`
    - `attempt_started`
    - `attempt_failed`
    - `retry_scheduled`
    - `attempt_succeeded`
    - `job_failed`
    - `job_succeeded`
    - `recovered_as_failed`
  - job orchestration 异常收口已补：
    - 如果 worker 已返回，但后续 summary / artifact 冻结阶段抛异常，job 会直接持久化为 `failed`
    - 不再先写出“finished 但仍是 running”的短暂脏状态
  - `README.md` 已补 article 后端最小运行说明
  - article 本地交付与运维收口已继续推进：
    - `README.md` 已补本地安装、console scripts、首次启动顺序和 runbook
    - packaging 元数据现在有 console script target 合同测试
    - `article-local --help` 与各 alias `--help` 已补合同测试
    - `article-doctor` 现在输出 headline / issues / recommended actions / workflow 命令模板
    - `article-local init` 已补：
      - 初始化 SQLite 状态库
      - 预创建 `jobs / uploads / staging`
      - 可选写出 env 文件
      - 直接返回 `doctor / serve` 下一步命令
    - repo 级安装脚本已补：
      - `python3 scripts/install_article_local.py`
      - 默认创建 `~/.article/venv`
      - 安装当前仓库的 `.[api]`
      - 可选 `--no-deps --skip-init` 做无网络安装链路校验
      - `--python` 现在支持直接传 `PATH` 里的解释器名
      - `article-local init` 若写出同内容 env 文件，重复执行不再报错
      - `init` 非 JSON stdout 现在会被清晰收口成安装错误
    - local shell env/runbook 稳定性已继续补强：
      - env 文件与 workflow 命令现在做 shell-safe quoting
      - 从已安装 venv 执行 `article-local init --write-env` 时，env 文件会补 `PATH`
      - `article-local serve` 结束后不再残留 root 环境变量
    - local shell 显式 root 覆盖不再污染进程级环境变量
    - article 测试默认隔离 state/runtime roots，避免全量回归时被外部测试顺序污染
  - article 后端全套定向回归：`148 passed`
  - FastAPI HTTP smoke：`6 passed`
  - 全量回归：`523 passed, 6 skipped, 1 xfailed`

## 最新真实产物

- `outputs/毕业论文初稿_含第四章_新工具修复版_v13_图规则主链验证.docx`
- `山_论文_最新助手修复_优化图表引用_v6_参考文献复核版.docx`
- `毕业论文初稿_含第四章_新工具修复版_v15_参考文献标题与格式修正.docx`
- `outputs/序言_第1章_优化修改版_新文献格式定稿_含第2章_layout_rebalance.docx`

## 当前未完全主链化

- 参考文献标题占位符（如 `——`）自动识别并标准化为 `参考文献`
- 参考文献内容层的深度语义规范
- frontmatter / TOC / backmatter 仍有少量重复检测逻辑可继续收束，但应坚持一次只做一个小 helper
- 组合图“缩放后仍保证可识读”的尺寸策略仍以人工复核为主，尚未自动化
- 图表对象块的“跨页省页重排”已提供显式开关 `--layout-rebalance`
- 当前仍是启发式重排，不会读取页面剩余高度，也不会默认改动旧流程
- 如果真实文档里仍有跨页大空白，优先先用 `figures_tables + --layout-rebalance` 再看是否需要人工微调
- `article` 后端仍未做：
  - 前端
  - 多 worker / Redis / Celery
  - cancel / priority / queue backpressure
  - 更完整的 lease / heartbeat 恢复语义
  - lifecycle 状态扩容
  - retention 策略更细的参数化与更稳的后台调度
  - 更完整的结构化日志与观测字段
  - 更长的多版本 migration 链与更正式的存储维护策略
  - 非 editable 的本地安装/升级脚本与更正式的交付包装

## 当前工程风险

- Git 首个基线提交已经建立：`1a49046`
- 当前可以直接用 Git 精确查看每轮后续差异
- 但回归仍然主要靠测试和真实论文烟测，不靠 Git 替代验证
- 本轮 article 改动集中在 `scripts/article_api/`、`tests/test_article_*`、`README.md` 和记忆文件
- 依赖 SQLite 状态根的 article 套件不要并行跑；并行执行会复现已知共享状态污染，导致假失败
- 共享主链未做大规模行为调整，稳定性仍以全量 pytest 通过作为准绳
- 下一轮执行优先级已经明确：
  - 先继续 article 专属层
  - 不把 article 需求下沉到 thesis 共享核心

## 下次继续前先读

1. `项目修改日志.md`
2. `记忆机制.md`
3. `Git使用规范.md`
4. `references/lnu/lnu-implementation-memory.md`
5. `README.md`
6. `记忆/专项规则/article/01-后端阶段进度与交接_2026-04-19.md`
7. `plans/article-backend-optimization-plan-2026-04-19.md`

## 当前判断标准

- 新论文再出现图规则、表块留白/表题贴表规则、参考文献版式的同类问题，优先判定为工具问题
- 新论文再出现“正常分析句被误当成独立图表引导句”或“表块/图块没有跟到真实引用后面”的问题，也优先判定为工具问题
- 只有学校样本里新增了明确要求，或者出现新的异常文档结构，才再讨论是否属于规则缺口或个例
- 项目级规则、代码、测试和记忆文件进入 Git；`outputs/` 产物不进入 Git
- 继续重构时，只允许低风险内聚化，不允许为了“代码更漂亮”去改主链行为
- `article` 后端下一轮也继续按这个标准：先稳合同，再谈功能面，不提前跳前端
