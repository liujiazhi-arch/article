# 项目修改日志

## 项目定位

本项目是一个面向中文论文 `.docx` 的结构化审查、定向修复和复查工具。当前主目标不是生成论文，而是把学校格式要求沉淀成可验证、可回归、可按 scope 执行的本地格式优化工具。

## 当前主链

- 推荐入口：`scripts/thesis_workbench.py`
- 工作流编排：`scripts/thesis_tool/workflow.py`
- 审查运行时：`scripts/audit_thesis.py`
- 修复运行时：`scripts/fix_thesis.py`
- 文档模型与段落分类：`scripts/_thesis_utils.py`
- profile 解析：`scripts/_profile_utils.py`
- 能力矩阵：`config/capability_matrix.md`

不要再把 `format_thesis.py`、`fix_and_preview.py`、`scripts/legacy/` 当成主流程。

## Profile 状态

- 通用 profile：`cn-common`
- 辽大唯一主 profile：`config/profiles/lnu-checker-2026.yaml`
- 辽大可用入口：`lnu`、`lnu-checker`、`lnu-checker-2026`
- 已删除：`config/profiles/lnu-undergraduate.yaml`
- 已删除：`config/profiles/ams-graduate.yaml`
- 已删除：`lnu-undergraduate` 运行时别名
- 旧辽大样本与 2026-04-17 checker 基线只作为历史参考；若与用户 2026-05-01 最新截图冲突，以最新截图和当前 profile 为准。

## 当前规则规模

- `cn-common` runtime：49 条规则
- `lnu` runtime：73 条规则
- 最新全量测试基线：`523 passed, 6 skipped, 1 xfailed`

## 已稳定主链化的能力

- 按 scope 执行 `audit / plan / apply / verify / diagnose`
- 页面、摘要、目录、标题、正文、图表、参考文献、致谢、附录的分区处理
- 辽大正文与标题样式参数化
- 辽大目录结构识别与 TOC 域重建
- 辽大序言、标题链和误套 Heading 样式的诊断/修复
- 图块按“图片本体 + 图名 + 图注”整体处理
- 表块按“表名 + 表格 + 表注”整体处理
- 公式布局表边框保护，避免被误套三线表
- 普通非一级标题的历史 `pageBreakBefore` 清理
- 图表对象块显式 `--layout-rebalance` 重排
- 参考文献编号后空格、悬挂缩进、字号、行距、段距与标点专项规则
- 摘要/目录/正文混排空格紧凑化：
  - 中文正文中英文、中文数字之间不保留空格
  - 数字和单位之间保留空格
  - 标题编号如 `1.2 标题` 保留编号后空格
  - 英文摘要与英文关键词保留英文半角标点

## 当前不自动处理的边界

- 封面默认不改，除非用户明确要求
- 自动目录页码刷新仍依赖 Word/WPS 更新域
- 图表紧凑排版仍是启发式，不读取真实页面剩余高度
- 组合图缩放后是否可读仍需人工复核
- 参考文献标题占位符如 `——` 自动标准化尚未完全主链化
- 参考文献内容层深度语义规范尚未完全自动化
- 野生文档自动判别与修整仍是下一阶段重点
- WPS/Word 渲染闭环仍需继续工程化

## 重要工程原则

- profile 写了规则，不等于规则已接入 runtime；新增 active `additions` 必须同步 audit、fix、scope、能力矩阵和测试。
- 能稳定结构化判断的规则优先进入 `audit + fix + verify`。
- 高语义风险规则先做诊断或人工复核，不强行自动化。
- 图、表、公式、目录、参考文献属于对象块，不能只修单个文本段。
- Git 保存历史，记忆文件只保存当前事实和少量稳定经验；不要再把每轮对话流水写进长期记忆。
- 代码和最近 Git 提交优先级高于旧记忆文件。

## Article 后端状态

article 后端已经形成本地原型，具备：

- `article-local` 统一入口
- `article-api`、`article-doctor`、`article-maintain`、`article-backup`、`article-restore`
- profile catalog
- batch audit/plan/verify/apply
- job、upload、retry、cleanup、retention、ready/version/ops summary
- SQLite schema migration、维护、备份恢复
- wheel 安装、upgrade、rollback 骨架

当前定位仍是本地单机后端/运行壳，不是完整前端产品。后续应优先收口使用体验、渲染闭环和真实文档稳定性，不要把 article 需求下沉污染 thesis 共享核心。

## 下一阶段主目标

1. 野生文档识别：自动判断手工目录/自动目录、标题样式污染、参考文献边界、附录/致谢边界、图表对象块异常。
2. 渲染闭环：减少 Word/WPS computer-use 反复找控件的 token 浪费，建立可重复的导出/复核流程。
3. 图表紧凑排版：在现有 `--layout-rebalance` 基础上继续改善对象块锚点、分页保护和留白判断。
4. 参考文献标题占位符主链化：把 `——`、空段等异常标题稳定审出并修正。
5. 文档结构清理：README 后续可拆出 article runbook、WPS 复核说明和 profile 说明，避免单文件继续膨胀。

## 最近提交摘要

- `2512a21 chore(profiles): remove retired lnu undergraduate alias`
- `bf0dd8b docs: document lnu workflow and project context`
- `484dbed test: harden article and thesis workflow coverage`
- `b991ea8 fix(references): stop before appendix variants`
- `2ac28de feat(lnu): consolidate latest checker profile`

更早的详细流水保留在 Git 历史里，不再复制进长期记忆。
