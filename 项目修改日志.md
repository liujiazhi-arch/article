# 论文格式检查工具项目修改日志

## 项目定位
- 本项目用于对中文毕业论文 `.docx` 执行结构化审查、按 scope 定向修复、复查验证，并逐步将学校样本中的格式要求沉淀为可执行规则。
- 当前主目标不是“生成一篇论文”，而是“稳定地审、稳妥地改、能回归验证、尽量减少重复返工”。

## 使用约定
- 本文件是项目级长期记忆，只在“大型项目级修改”时查看和更新。
- 所谓大型项目级修改，指的是：
- 新增或重构某条学校规则
- 调整主链修复策略
- 修改 scope、capability、audit/fix 接入方式
- 沉淀一次明确的根因复盘和工程经验
- 日常使用 `audit / apply / verify / diagnose` 跑具体论文时，不默认读取、不依赖本文件。
- 只有当用户明确要求“改规则”“查根因”“看当前项目改到哪儿”“继续沿着项目经验迭代”时，才把本文件作为上下文入口。
- 本文件必须迭代式维护，新增内容应追加为新的阶段、规则或经验，不覆盖掉已有项目记忆。

## 记忆机制
- 当前项目的长期记忆机制已经固定，不再只依赖单篇论文输出日志。
- 记忆文件分层如下：
- `项目修改日志.md`
  - 项目总账，记录主链能力、里程碑、验证结果、下一步建议
- `当前交接状态.md`
  - 给下一次接手的 Codex 快速读的短状态
- `references/lnu/lnu-implementation-memory.md`
  - 辽大专项深度经验
- `记忆/专项规则/lnu/`
  - 按章节拆分的辽大规则总表与专项规则文件
- `记忆/每日记录/YYYY-MM-DD.md`
  - 每日优化记录
- `outputs/*.md`
  - 单文档局部日志，不视为长期唯一入口
- 后续继续本项目时，默认先读：
- `项目修改日志.md`
- `当前交接状态.md`
- 对应专项记忆
- 对应章节专项规则文件
- 新论文再出现已经主链化的问题时，优先视为工具问题，而不是重新讨论审查口径。

## 当前状态
- 已具备 `plan / apply / audit / diagnose / verify / scopes` 主链工作流。
- 已支持按 `page / abstract / toc / headings / body_paragraphs / figures_tables / references / acknowledgement / appendix` 分 scope 修复。
- 已形成辽宁大学 `lnu` profile，覆盖正文、标题、目录、参考文献、图表、致谢等主要格式要求。
- 已建立测试体系，当前围绕主链、辽大规则和图表修复的回归测试已通过。

## 核心流程
- `audit`：执行全文规则审查，输出当前未通过项。
- `plan`：按 scope 汇总问题，区分可自动修复、需人工复核、当前不支持。
- `apply`：按选定 scope 执行修复，避免无关区域被误改。
- `verify`：对修复范围复查，确认当前 scope 是否已经稳定落地。
- `diagnose`：在辽大规则疑难场景下辅助定位文档结构问题。

## 已沉淀的主链原则
- 能结构化判断、低误判的规则，优先接入 `audit + fix + verify` 主链。
- 语义依赖强、误改风险高的规则，继续保留人工复核，不强行自动化。
- 图、表、目录、参考文献这类“对象块”，必须按块处理，不能只修其中一个文本段。
- Word 文档中的 `drawing-only` 段落必须视为结构对象，不能因无文字而跳过。
- 留白规则不能只写死某一段的 `before/after`，需要与相邻标题或正文的现有段距一起结算。

## 关键里程碑

### 第一阶段：正文与标题主链稳定
- 摘要、正文、标题、目录、参考文献、致谢等基础格式能力已进入主链。
- 已完成正文样式参数化：中文宋体、英文与数字 Times New Roman、小四、1.5 倍行距、首行缩进 2 字符、两端对齐。
- 已完成标题体系参数化：一级到四级标题的字体、字号、加粗与段前后距统一由 profile 驱动。
- 已形成按 scope 定向修复能力，避免一把梭修改全篇导致副作用。

### 第二阶段：图表与实验结果结构修复
- 已接入 `LNU_F05`：图表需先文中引用。
- 已修复实验结果章节“分析文字在图后”的问题，当前策略为先前移分析文字，再整体归位图块。
- 已将“图片本体 + 图名 + 图注”统一视为完整图块处理，避免只移动图题导致视觉顺序仍错乱。

### 第三阶段：图规则正式接入主链
- 已新增并接入 `LNU_F03`：图与上文留一空行，图名/图注与下文留一空行。
- 已新增并接入 `LNU_F06`：图题五号居中、1.5 倍行距；图注五号、单倍行距。
- 已修复自动补前文引用时插入位置错误的问题，补入语句现在会出现在整个图块之前，不再插到图片与图名之间。
- 已将图题与图注拆分处理，图注不再沿用图题行距逻辑。

### 第四阶段：辽大参考文献规则正式收口
- 已将辽大参考文献的编号、对齐、段距与行距要求显式写入 `lnu` profile，不再继续吃通用默认值。
- 已确认辽大参考文献采用 `[N] + 单空格`，不使用 `Tab`，不允许补零编号 `[01]`。
- 已将悬挂缩进参数固定为 `420 / -420`，替代此前误用的默认 `560 / -560`。
- 已将“宋体五号、英文 `Times New Roman`、1.5 倍行距、段前后 0”接入参考文献专项审查与修复。
- 已在真实文档中确认：参考文献标题本身也可能是事故点，可能出现 `——` 这种占位符而不是标准 `参考文献`。

### 第五阶段：辽大章节专项拆分完成首轮落地
- 已根据 2026-04-09 截图组，按章节建立辽大规则专项拆分，不再只保留在聊天上下文中。
- 已建立：
  - `记忆/专项规则/lnu/00-章节规则总表.md`
  - `记忆/专项规则/lnu/01-前置部分.md`
  - `记忆/专项规则/lnu/02-正文与对象块.md`
  - `记忆/专项规则/lnu/03-后置部分.md`
- 当前章节专项拆分已覆盖：
  - 中文摘要
  - 英文摘要
  - 目录
  - 序言
  - 正文普通段落
  - 图
  - 图名
  - 图注
  - 表
  - 表名
  - 表注
  - 公式
  - 参考文献
  - 致谢
  - 附录
  - 封面与页眉页脚的当前状态说明

### 第六阶段：Git 基础设施与记忆双轨正式落地
- 已确认本项目后续采用“记忆文件 + Git”双轨机制，而不是二选一。
- 已补齐项目级 `.gitignore`，明确忽略 `outputs/` 生成产物、缓存、临时文件和系统噪音。
- 已新增 `.gitattributes`，将 `.docx` 等二进制产物标记为 binary，避免无意义文本 diff。
- 已新增 `Git使用规范.md`，把本项目适用的分支、提交粒度、收尾动作和提交边界固定下来。
- 当前推荐 Git 只追踪代码、配置、测试和记忆文件，不追踪单篇论文的生成结果。

### 第七阶段：输出分层纳管进入 Git 设计
- 已将 `outputs/` 从“全部忽略”升级为“默认忽略、索引纳管、里程碑提升”的三层策略。
- 已新增 `scripts/output_versioning.py`，用于刷新输出索引和提升里程碑产物。
- 已新增 `outputs/index/`，用于让 Git 看见当前输出结果与里程碑记录。
- 已新增 `outputs/milestones/`，用于只追踪少量关键 `.docx` 成果。
- 当前策略目标是：增强版本控制颗粒度，但不污染主链历史，也不替代记忆机制。

### 第八阶段：frontmatter / TOC 共享检测逻辑继续内聚
- 已新增 `scripts/frontmatter_utils.py` 的目录语义辅助能力，统一承接：
  - 关键词识别
  - TOC 域指令检测
  - TOC 生成样式 / 结构样式识别
  - TOC 标题样式 / 条目样式语义识别
- 已将 `_thesis_utils.py`、`audit_thesis.py`、`thesis_tool/workflow.py` 中与 TOC/frontmatter 相关的底层判断继续收束到共享 helper，减少局部重复实现。
- 本轮继续抽出了“连续目录块范围识别”共享 helper，`repair_misplaced_abstract_keywords()` 与 `remove_existing_toc_artifacts()` 已改为共用同一套块边界检测，不再各写一套起止判断。
- 本轮刻意没有扩张修复策略，只做检测逻辑内聚；目标是降低后续继续处理摘要/目录/分页联动时的维护成本，同时不碰主链行为。

## 本轮重要经验

### 图表相关
- 图规则最容易反复的根因，不是规则没写，而是图块识别不完整。
- 只检查“图题前是否出现图号引用”不够，必须同时检查图块前后文顺序和图块上下留白。
- 图块修复完成后，必须再次统一结算图前图后间距，否则前文引用补语或标题段距会把布局再次打乱。

### 工程相关
- profile 里写了规则，不等于规则真的进入 runtime；必须同时接入：
- `config/profiles/*.yaml`
- `audit_thesis.py`
- `fix_thesis.py`
- `thesis_tool/scopes.py`
- `config/capability_matrix.md`
- 对同一规则不能保留多套并行实现；最终应清理为一条主链路径，避免后续维护出现“改了一处，另一处没改”。
- 对这个项目，`Git` 最适合记录“主链如何演进”，不适合替代“下一次先读什么”的记忆文件；两者必须分工明确。
- `outputs/` 里的 `.docx` 和单次局部日志默认不应进入版本历史，否则会迅速污染提交记录，降低规则演进的可读性。
- 如果希望 Git 也能“看见结果”，优先追踪输出索引和少量里程碑文件，而不是把全部中间产物直接纳入版本控制。
- 当前仓库虽然已建立 `.gitignore`、`.gitattributes` 和 Git 使用规范，但尚未形成首个基线提交；因此 Git 目前还不能精确告诉我们“本轮到底改了什么”，真正的回归兜底仍必须依赖测试和真实论文烟测。
- 在首个基线提交建立之前，任何小步重构都必须坚持：
- 先做低风险抽取
- 立即跑针对性测试
- 再跑全量测试
- 再做真实论文 `audit / verify`
- 只有全部通过，才算主链仍稳定

### TOC / frontmatter 相关
- 目录和前置部分的风险点，不在于某一条规则本身，而在于“目录标题 / 目录条目 / TOC 域 / 结构样式 / 空白段”往往共同构成一个连续块。
- 这类连续块如果各处各写一套识别逻辑，后续极易出现：
- 一处把空白段算进目录块
- 另一处没算进去
- 一处识别 TOCField
- 另一处只认可见文本
- 因此本轮明确将“连续目录块范围识别”沉淀为共享 helper，后续继续处理 frontmatter 分页、目录清理和摘要关键词归位时，优先复用这一层，而不是再局部复制判断。

### 参考文献相关
- “悬挂缩进方向正确”不等于“观感正确”；辽大参考文献虽然本来就该用悬挂缩进，但默认 `560 twips` 观感偏深，真实样图更接近 `420 twips`。
- 参考文献编号规则不能只修前导零，还要同时收掉编号后的多空格；否则前半段与后半段会继续视觉不一致。
- 学校专属参数写进 profile 后，必须确认运行时真的加载到了；本轮就暴露出 `ref_hanging` 虽然写入了 `lnu-undergraduate.yaml`，但 `build_profile_cfg()` 没有消费，最终仍继续用默认值的问题。
- 参考文献专项修复时，必要时应直接走底层 `fix_docx(..., scopes=['references'])`；因为 `thesis_workbench apply` 的后置全量 `verify` 目前可能被与参考文献无关的缺失 checker 干扰。
- 参考文献标题本身也是事故点；条目格式正确，不代表标题一定正确。真实文档里可能出现 `——`、空段或其他占位文本，需要单独检查。

## 当前已自动化、可放心走主链的图规则
- 图表在正文中先被引用，再出现图表。
- 实验结果章节中，分析文字可前移到图块之前。
- 图块上下留白按辽大要求统一。
- 图题与图注字体、字号、行距按辽大要求统一。
- 图题编号按 `图X.X`，表题编号按 `表X.X`。

## 当前已自动化、可放心走主链的参考文献规则
- 参考文献编号统一为 `[N]`，禁止 `[01]` 这类补零编号。
- 编号后统一为单个半角空格，拒绝多个空格和 `Tab`。
- 参考文献条目按辽大参数应用悬挂缩进 `420 / -420`。
- 参考文献条目统一为宋体五号，英文与数字 `Times New Roman`。
- 参考文献条目统一为 `1.5` 倍行距，段前 `0`，段后 `0`。
- 参考文献英文半角标点规则已接入辽大专项检查。

## 当前仍保留人工复核的边界
- 图内附注是否必须写成 `注1）` 这种细粒度编号格式。
- 图注是否充分解释 `A/B/C` 子图、符号、统计字母、缩写等语义信息。
- 图是否跨页、图名是否与图片同页、图是否因 Word 浮动布局造成视觉异常。
- 这类问题多与渲染或语义有关，当前不宜强行自动化。
- 参考文献标题占位符（如 `——`）的自动识别与标准化，当前尚未完全主链化，真实文档中仍可能需要单独补正。
- 参考文献内容层面的深度语义规范，例如作者缩写风格、期刊名大小写、在线发表条目写法，目前仍未完全自动化。

## 已知文件与产物约定
- 工具主说明：`README.md`
- profile 配置：`config/profiles/`
- 主链脚本：`scripts/thesis_workbench.py`
- 审计逻辑：`scripts/audit_thesis.py`
- 修复逻辑：`scripts/fix_thesis.py`
- 主链测试：`tests/`
- 文档产物：`outputs/`
- 输出索引与里程碑控制：`scripts/output_versioning.py`
- Git 使用约定：`Git使用规范.md`

## 维护建议
- 以后每次新增学校规则，优先判断它属于：
- 可稳定自动化
- 可半自动定位但需人工确认
- 暂时不适合自动化
- 每次规则接入时都要补：
- 一条 profile 规则
- 一条 audit checker
- 对应 fix 接口或明确保持 `null`
- 至少一个最小回归测试
- 一条项目日志记录
- 不再把项目级经验只写在某一篇论文的输出日志里，应统一沉淀到本文件。

## 最近一次验证
- 图规则主链接入后，相关回归测试已通过。
- 基于新主链重新生成的图规则验证文件：
- `outputs/毕业论文初稿_含第四章_新工具修复版_v13_图规则主链验证.docx`
- 该文件在 `figures_tables` scope 下复查通过，图规则未发现问题。
- 参考文献专项已完成真实文档验证：
- `山_论文_最新助手修复_优化图表引用_v6_参考文献复核版.docx`
- `毕业论文初稿_含第四章_新工具修复版_v15_参考文献标题与格式修正.docx`
- 抽查结果已确认：
- 标题 `参考文献` 居中
- 条目参数为 `left_indent=420`、`first_line_indent=-420`、`space_before=0`、`space_after=0`、`line_spacing=1.5`
- frontmatter / TOC 共享化整理后的最新验证结果：
- `python3 -m pytest -q`：`218 passed`
- 真实论文 `audit`：`outputs/毕业论文初稿_含第四章_v15_完整论文验证修复_ref_retry.docx` -> `100/100`
- 真实论文 `verify`：`outputs/毕业论文初稿_含第四章_v15_完整论文验证修复_ref_retry.docx` -> `100/100`
- 本轮新增的保护测试位于 `tests/test_frontmatter_utils.py`，用于锁定“连续目录块范围识别”的边界行为。

## 下一步建议
- 优先建立 Git 首个基线提交；否则后续再做任何结构整理，都很难借助 Git 精确审查本轮差异。
- 在不改主链行为的前提下，继续把 frontmatter / TOC / backmatter 的“共享检测逻辑”往 helper 层收束，但每次只允许做一小步抽取，并且必须附带保护测试。
- 继续补强参考文献主链，优先把“标题占位符 -> 标准参考文献标题”的识别与修复正式接入主链。
- 继续补强参考文献内容层规则，例如作者缩写、期刊名大小写、在线发表条目格式，但这些应与版式规则分层推进。
- 将更多“图块/表块级”布局规则统一进同一套块处理框架，减少特殊脚本。
- 如果后续要扩展到更多学校，优先复用现有主链，只在 profile 和少量学校特化规则层做增量。
- 现在可以开始用 Git 管理项目级修改；建议第一批提交优先拆成“基础设施 / 记忆文件 / 规则能力”三类，不要把生成论文产物提交进版本库。
- 后续每轮重要文档处理结束后，可按需刷新 `outputs/index/最新产物索引.md`；只有少数回归价值高的产物才提升到 `outputs/milestones/`。
