# 论文格式检查工具项目修改日志

## 项目定位
- 本项目用于对中文毕业论文 `.docx` 执行结构化审查、按 scope 定向修复、复查验证，并逐步将学校样本中的格式要求沉淀为可执行规则。
- 当前主目标不是“生成一篇论文”，而是“稳定地审、稳妥地改、能回归验证、尽量减少重复返工”。

## 使用约定
- 本文件是项目级长期记忆，只在“大型项目级修改”时查看和更新。
- 所谓大型项目级修改，指的是：
- 新增或重构某条学校规则
- 调整主链修复策略
- 修改 scope、capability、audit/fix 接入方式
- 沉淀一次明确的根因复盘和工程经验
- 日常使用 `audit / apply / verify / diagnose` 跑具体论文时，不默认读取、不依赖本文件。
- 只有当用户明确要求“改规则”“查根因”“看当前项目改到哪儿”“继续沿着项目经验迭代”时，才把本文件作为上下文入口。
- 本文件必须迭代式维护，新增内容应追加为新的阶段、规则或经验，不覆盖掉已有项目记忆。

## 记忆机制
- 当前项目的长期记忆机制已经固定，不再只依赖单篇论文输出日志。
- 记忆文件分层如下：
- `项目修改日志.md`
  - 项目总账，记录主链能力、里程碑、验证结果、下一步建议
- `当前交接状态.md`
  - 给下一次接手的 Codex 快速读的短状态
- `references/lnu/lnu-implementation-memory.md`
  - 辽大专项深度经验
- `记忆/专项规则/lnu/`
  - 按章节拆分的辽大规则总表与专项规则文件
- `记忆/每日记录/YYYY-MM-DD.md`
  - 每日优化记录
- `outputs/*.md`
  - 单文档局部日志，不视为长期唯一入口
- 后续继续本项目时，默认先读：
- `项目修改日志.md`
- `当前交接状态.md`
- 对应专项记忆
- 对应章节专项规则文件
- 新论文再出现已经主链化的问题时，优先视为工具问题，而不是重新讨论审查口径。

## 当前状态
- 已具备 `plan / apply / audit / diagnose / verify / scopes` 主链工作流。
- 已支持按 `page / abstract / toc / headings / body_paragraphs / figures_tables / references / acknowledgement / appendix` 分 scope 修复。
- 已形成辽宁大学 `lnu` profile，覆盖正文、标题、目录、参考文献、图表、致谢等主要格式要求。
- 已建立测试体系，当前围绕主链、辽大规则和图表修复的回归测试已通过。

## 核心流程
- `audit`：执行全文规则审查，输出当前未通过项。
- `plan`：按 scope 汇总问题，区分可自动修复、需人工复核、当前不支持。
- `apply`：按选定 scope 执行修复，避免无关区域被误改。
- `verify`：对修复范围复查，确认当前 scope 是否已经稳定落地。
- `diagnose`：在辽大规则疑难场景下辅助定位文档结构问题。

## 已沉淀的主链原则
- 能结构化判断、低误判的规则，优先接入 `audit + fix + verify` 主链。
- 语义依赖强、误改风险高的规则，继续保留人工复核，不强行自动化。
- 图、表、目录、参考文献这类“对象块”，必须按块处理，不能只修其中一个文本段。
- Word 文档中的 `drawing-only` 段落必须视为结构对象，不能因无文字而跳过。
- 留白规则不能只写死某一段的 `before/after`，需要与相邻标题或正文的现有段距一起结算。

## 关键里程碑

### 第一阶段：正文与标题主链稳定
- 摘要、正文、标题、目录、参考文献、致谢等基础格式能力已进入主链。
- 已完成正文样式参数化：中文宋体、英文与数字 Times New Roman、小四、1.5 倍行距、首行缩进 2 字符、两端对齐。
- 已完成标题体系参数化：一级到四级标题的字体、字号、加粗与段前后距统一由 profile 驱动。
- 已形成按 scope 定向修复能力，避免一把梭修改全篇导致副作用。

### 第二阶段：图表与实验结果结构修复
- 已接入 `LNU_F05`：图表需先文中引用。
- 已修复实验结果章节“分析文字在图后”的问题，当前策略为先前移分析文字，再整体归位图块。
- 已将“图片本体 + 图名 + 图注”统一视为完整图块处理，避免只移动图题导致视觉顺序仍错乱。
- 已新增“显式启用的图表跨页排布优化”流程开关：
  - 默认关闭
  - 仅在 `figures_tables` scope 下生效
  - 当前启发式版本会按同小节内已有图号引用，将图块前移到更合理的位置，以减少跨页大留白
- 该功能刻意不并入默认主链，避免对旧论文和旧测试造成隐式重排副作用。

### 第三阶段：图规则正式接入主链
- 已新增并接入 `LNU_F03`：图与上文留一空行，图名/图注与下文留一空行。
- 已新增并接入 `LNU_F06`：图题五号居中、1.5 倍行距；图注五号、单倍行距。
- 已修复自动补前文引用时插入位置错误的问题，补入语句现在会出现在整个图块之前，不再插到图片与图名之间。
- 已将图题与图注拆分处理，图注不再沿用图题行距逻辑。

### 第三阶段补充：表块留白规则正式接入主链
- 已新增并接入 `LNU_TB04`：表块与上下文留一空行，表题紧贴表体。
- 当前 runtime 会将“表题 + 表格 + 表注”视为完整表块处理，而不是只修表名字号。
- 已修复两类高频问题：
  - 表题与表格之间被空白段或段后距拉开
  - 表格结束后与下文或后续图块紧挨在一起
- 新规则已补入：
  - `config/profiles/lnu-checker-2026.yaml`
  - `scripts/audit_thesis.py`
  - `scripts/fix_thesis.py`
  - `config/capability_matrix.md`
  - 对应回归测试

### 第四阶段：辽大参考文献规则正式收口
- 已将辽大参考文献的编号、对齐、段距与行距要求显式写入 `lnu` profile，不再继续吃通用默认值。
- 已确认辽大参考文献采用 `[N]` 后直接接正文内容，不使用 `Tab`，不允许补零编号 `[01]`。
- 已将悬挂缩进参数固定为 `420 / -420`，替代此前误用的默认 `560 / -560`。
- 已将“宋体五号、英文 `Times New Roman`、1.5 倍行距、段前后 0”接入参考文献专项审查与修复。
- 已在真实文档中确认：参考文献标题本身也可能是事故点，可能出现 `——` 这种占位符而不是标准 `参考文献`。

### 第五阶段：辽大章节专项拆分完成首轮落地
- 已根据 2026-04-09 截图组，按章节建立辽大规则专项拆分，不再只保留在聊天上下文中。
- 已建立：
  - `记忆/专项规则/lnu/00-章节规则总表.md`
  - `记忆/专项规则/lnu/01-前置部分.md`
  - `记忆/专项规则/lnu/02-正文与对象块.md`
  - `记忆/专项规则/lnu/03-后置部分.md`
- 当前章节专项拆分已覆盖：
  - 中文摘要
  - 英文摘要
  - 目录
  - 序言
  - 正文普通段落
  - 图
  - 图名
  - 图注
  - 表
  - 表名
  - 表注
  - 公式
  - 参考文献
  - 致谢
  - 附录
  - 封面与页眉页脚的当前状态说明

### 第六阶段：Git 基础设施与记忆双轨正式落地
- 已确认本项目后续采用“记忆文件 + Git”双轨机制，而不是二选一。
- 已补齐项目级 `.gitignore`，明确忽略 `outputs/` 生成产物、缓存、临时文件和系统噪音。
- 已新增 `.gitattributes`，将 `.docx` 等二进制产物标记为 binary，避免无意义文本 diff。
- 已新增 `Git使用规范.md`，把本项目适用的分支、提交粒度、收尾动作和提交边界固定下来。
- 当前推荐 Git 只追踪代码、配置、测试和记忆文件，不追踪单篇论文的生成结果。
- 已建立首个 Git 基线提交：`1a49046` (`chore: establish baseline for thesis formatting tool`)。
- 从这一刻起，后续每轮优化都可以用 Git 精确追踪真实差异，不再处于“整仓都是未提交基线”的状态。

### 第七阶段：输出分层纳管进入 Git 设计
- 已将 `outputs/` 从“全部忽略”升级为“默认忽略、索引纳管、里程碑提升”的三层策略。
- 已新增 `scripts/output_versioning.py`，用于刷新输出索引和提升里程碑产物。
- 已新增 `outputs/index/`，用于让 Git 看见当前输出结果与里程碑记录。
- 已新增 `outputs/milestones/`，用于只追踪少量关键 `.docx` 成果。
- 当前策略目标是：增强版本控制颗粒度，但不污染主链历史，也不替代记忆机制。

### 第八阶段：frontmatter / TOC 共享检测逻辑继续内聚
- 已新增 `scripts/frontmatter_utils.py` 的目录语义辅助能力，统一承接：
  - 关键词识别
  - TOC 域指令检测
  - TOC 生成样式 / 结构样式识别
  - TOC 标题样式 / 条目样式语义识别
- 已将 `_thesis_utils.py`、`audit_thesis.py`、`thesis_tool/workflow.py` 中与 TOC/frontmatter 相关的底层判断继续收束到共享 helper，减少局部重复实现。
- 本轮继续抽出了“连续目录块范围识别”共享 helper，`repair_misplaced_abstract_keywords()` 与 `remove_existing_toc_artifacts()` 已改为共用同一套块边界检测，不再各写一套起止判断。
- 本轮刻意没有扩张修复策略，只做检测逻辑内聚；目标是降低后续继续处理摘要/目录/分页联动时的维护成本，同时不碰主链行为。

## 本轮重要经验

### 图表相关
- 图规则最容易反复的根因，不是规则没写，而是图块识别不完整。
- 只检查“图题前是否出现图号引用”不够，必须同时检查图块前后文顺序和图块上下留白。
- 图块修复完成后，必须再次统一结算图前图后间距，否则前文引用补语或标题段距会把布局再次打乱。
- 表规则也遵循同一原则：不能把“表名”当成一个独立文本段处理，必须把表名前后留白和表体后的下文间隔一起结算。
- 2026-04-14 起，若新论文仍出现“表名离表太远”“表后与图/正文挤在一起”，优先判定为工具问题而不是人工排版疏漏。

### 工程相关
- profile 里写了规则，不等于规则真的进入 runtime；必须同时接入：
- `config/profiles/*.yaml`
- `audit_thesis.py`
- `fix_thesis.py`
- `thesis_tool/scopes.py`
- `config/capability_matrix.md`
- 对同一规则不能保留多套并行实现；最终应清理为一条主链路径，避免后续维护出现“改了一处，另一处没改”。
- 对这个项目，`Git` 最适合记录“主链如何演进”，不适合替代“下一次先读什么”的记忆文件；两者必须分工明确。
- `outputs/` 里的 `.docx` 和单次局部日志默认不应进入版本历史，否则会迅速污染提交记录，降低规则演进的可读性。
- 如果希望 Git 也能“看见结果”，优先追踪输出索引和少量里程碑文件，而不是把全部中间产物直接纳入版本控制。
- Git 现在已经具备真实差异追踪能力，但回归兜底仍必须依赖测试和真实论文烟测，不能把 Git diff 当成质量验证本身。
- 在后续任何小步重构里，都必须坚持：
- 先做低风险抽取
- 立即跑针对性测试
- 再跑全量测试
- 再做真实论文 `audit / verify`
- 只有全部通过，才算主链仍稳定

### TOC / frontmatter 相关
- 目录和前置部分的风险点，不在于某一条规则本身，而在于“目录标题 / 目录条目 / TOC 域 / 结构样式 / 空白段”往往共同构成一个连续块。
- 这类连续块如果各处各写一套识别逻辑，后续极易出现：
- 一处把空白段算进目录块
- 另一处没算进去
- 一处识别 TOCField
- 另一处只认可见文本
- 因此本轮明确将“连续目录块范围识别”沉淀为共享 helper，后续继续处理 frontmatter 分页、目录清理和摘要关键词归位时，优先复用这一层，而不是再局部复制判断。

### 参考文献相关
- “悬挂缩进方向正确”不等于“观感正确”；辽大参考文献虽然本来就该用悬挂缩进，但默认 `560 twips` 观感偏深，真实样图更接近 `420 twips`。
- 参考文献编号规则不能只修前导零，还要同时收掉编号后的多空格；辽大最新主 profile 以 `[N]` 后无空格为准。
- 学校专属参数写进 profile 后，必须确认运行时真的加载到了；本轮就暴露出 `ref_hanging` 虽然写入了 profile，但 `build_profile_cfg()` 没有消费，最终仍继续用默认值的问题。
- 参考文献专项修复时，必要时应直接走底层 `fix_docx(..., scopes=['references'])`；因为 `thesis_workbench apply` 的后置全量 `verify` 目前可能被与参考文献无关的缺失 checker 干扰。
- 参考文献标题本身也是事故点；条目格式正确，不代表标题一定正确。真实文档里可能出现 `——`、空段或其他占位文本，需要单独检查。

## 当前已自动化、可放心走主链的图规则
- 图表在正文中先被引用，再出现图表。
- 实验结果章节中，分析文字可前移到图块之前。
- 图块上下留白按辽大要求统一。
- 图题与图注字体、字号、行距按辽大要求统一。
- 图题编号按 `图X.X`，表题编号按 `表X.X`。

## 当前已自动化、可放心走主链的表块规则
- 表题位于表格上方，并按 `表X.X` 编号。
- 表题与上文留一空行。
- 表题段后为 `0`，紧贴表格本体。
- 表块结束后与下文或后续图块留一空行。
- 表格内容保持宋体五号、单倍行距、居中对齐相关修复链不受影响。

## 当前已自动化、可放心走主链的参考文献规则
- 参考文献编号统一为 `[N]`，禁止 `[01]` 这类补零编号。
- 编号后不保留空格，拒绝多个空格和 `Tab`。
- 参考文献条目按辽大参数应用悬挂缩进 `420 / -420`。
- 参考文献条目统一为宋体五号，英文与数字 `Times New Roman`。
- 参考文献条目统一为 `1.5` 倍行距，段前 `0`，段后 `0`。
- 参考文献英文半角标点规则已接入辽大专项检查。

## 当前仍保留人工复核的边界
- 图内附注是否必须写成 `注1）` 这种细粒度编号格式。
- 图注是否充分解释 `A/B/C` 子图、符号、统计字母、缩写等语义信息。
- 图是否跨页、图名是否与图片同页、图是否因 Word 浮动布局造成视觉异常。
- 图表对象块在真实分页中的“省页重排”仍未自动化。
- 当前工具能修结构留白，但还不会根据页面剩余高度自动决定“图在表前还是表在图前”“总结段放图前还是图后”。
- 这类问题多与渲染或语义有关，当前不宜强行自动化。
- 参考文献标题占位符（如 `——`）的自动识别与标准化，当前尚未完全主链化，真实文档中仍可能需要单独补正。
- 参考文献内容层面的深度语义规范，例如作者缩写风格、期刊名大小写、在线发表条目写法，目前仍未完全自动化。

## 已知文件与产物约定
- 工具主说明：`README.md`
- profile 配置：`config/profiles/`
- 主链脚本：`scripts/thesis_workbench.py`
- 审计逻辑：`scripts/audit_thesis.py`
- 修复逻辑：`scripts/fix_thesis.py`
- 主链测试：`tests/`
- 文档产物：`outputs/`
- 输出索引与里程碑控制：`scripts/output_versioning.py`
- Git 使用约定：`Git使用规范.md`

## 维护建议
- 以后每次新增学校规则，优先判断它属于：
- 可稳定自动化
- 可半自动定位但需人工确认
- 暂时不适合自动化
- 每次规则接入时都要补：
- 一条 profile 规则
- 一条 audit checker
- 对应 fix 接口或明确保持 `null`
- 至少一个最小回归测试
- 一条项目日志记录
- 不再把项目级经验只写在某一篇论文的输出日志里，应统一沉淀到本文件。

## 最近一次验证
- 图规则主链接入后，相关回归测试已通过。
- 基于新主链重新生成的图规则验证文件：
- `outputs/毕业论文初稿_含第四章_新工具修复版_v13_图规则主链验证.docx`
- 该文件在 `figures_tables` scope 下复查通过，图规则未发现问题。
- 参考文献专项已完成真实文档验证：
- `山_论文_最新助手修复_优化图表引用_v6_参考文献复核版.docx`
- `毕业论文初稿_含第四章_新工具修复版_v15_参考文献标题与格式修正.docx`
- 抽查结果已确认：
- 标题 `参考文献` 居中
- 条目参数为 `left_indent=420`、`first_line_indent=-420`、`space_before=0`、`space_after=0`、`line_spacing=1.5`
- frontmatter / TOC 共享化整理后的最新验证结果：
- `python3 -m pytest -q`：`218 passed`
- 真实论文 `audit`：`outputs/毕业论文初稿_含第四章_v15_完整论文验证修复_ref_retry.docx` -> `100/100`
- 真实论文 `verify`：`outputs/毕业论文初稿_含第四章_v15_完整论文验证修复_ref_retry.docx` -> `100/100`
- 本轮新增的保护测试位于 `tests/test_frontmatter_utils.py`，用于锁定“连续目录块范围识别”的边界行为。
- 2026-04-14 追加完成图表跨页排布优化主链接入：
- `FixRuntime.layout_rebalance` 已接入 CLI / workflow / fix 主链，默认关闭，需显式传 `--layout-rebalance`
- `_is_generic_caption_lead_text()` 已收紧为“严格整句匹配”，不再把 `相关统计见表2.1。` 这类正常分析句误判成泛化引导句
- 表块与图块都支持按同小节真实引用锚点前移，减少跨页大空白
- `LNU_TB04` 已补入 `figures_tables` scope 映射
- `renumber_lnu_captions()` 已兼容 `2 结果` 这种数字章标题
- 全量回归已刷新为：`python3 -m pytest -q` -> `228 passed`
- 真实论文已生成启用该能力的验证产物：
- `outputs/序言_第1章_优化修改版_新文献格式定稿_含第2章_layout_rebalance.docx`

## 下一步建议
- 在不改主链行为的前提下，继续把 frontmatter / TOC / backmatter 的“共享检测逻辑”往 helper 层收束，但每次只允许做一小步抽取，并且必须附带保护测试。
- 继续补强参考文献主链，优先把“标题占位符 -> 标准参考文献标题”的识别与修复正式接入主链。
- 继续补强参考文献内容层规则，例如作者缩写、期刊名大小写、在线发表条目格式，但这些应与版式规则分层推进。
- 将更多“图块/表块级”布局规则统一进同一套块处理框架，减少特殊脚本。
- 如果后续要扩展到更多学校，优先复用现有主链，只在 profile 和少量学校特化规则层做增量。
- 现在可以开始用 Git 管理项目级修改；建议第一批提交优先拆成“基础设施 / 记忆文件 / 规则能力”三类，不要把生成论文产物提交进版本库。
- 后续每轮重要文档处理结束后，可按需刷新 `outputs/index/最新产物索引.md`；只有少数回归价值高的产物才提升到 `outputs/milestones/`。

### 第九阶段：Article 后端稳定化补强
- 已继续在 `article` 分身内收口后端原型的稳定性合同，重点不再是铺功能，而是修正 API 契约分叉、补强 recovery / retry / timeout 测试，并补最小运行说明。
- 已修正 fake app 与真实 FastAPI 在下载接口上的测试分叉：
  - fake app 测试现在显式禁用 `FileResponse` 分支
  - 真实 HTTP smoke 继续走真实下载响应
  - 避免出现“一边按 dict 断言，一边实际返回 `FileResponse`”的假绿/假红
- 已补强 job 合同测试：
  - stale `running` job 恢复后，会标记为 `failed + worker_recovery_failed`
  - 同时保留已有 `summary / result / artifacts` 冻结快照，不重算历史结果
  - `wait_for_job(timeout=...)` 已有明确测试保证它只代表调用侧等待超时，不直接改持久化状态
  - upload-backed job 在执行过 job cleanup 后，仍可基于 upload registry 重试
- 已收紧 cleanup 边界：
  - job cleanup 只负责 job workspace 与 job 产物
  - 不再顺手删除 upload registry 下的源文件
  - upload 生命周期继续由 upload cleanup 单独管理
- 已在 `README.md` 补入 article 后端原型的最小运行说明：
  - 启动命令
  - `ARTICLE_API_STATE_ROOT` 与 `runtime_root` 的区别
  - lifecycle / business status 的分层
  - cleanup / retry / snapshot 的合同
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py` -> `68 passed`
  - `/Users/apple/Desktop/article/.venv/bin/python -m pytest -q tests/test_article_http_smoke.py` -> `1 passed`
  - `python3 -m pytest -q` -> `380 passed, 1 xfailed`
- 当前判断：
  - article 后端本轮改动主要集中在 `scripts/article_api/` 与 `tests/test_article_*`
  - thesis 主链没有因为这轮后端补强被额外扩散修改
  - 全量回归通过，可认为共享链路未被大规模影响

### 第十阶段：Article 后端严格优化启动
- 已根据 agent team 方式把 article 后端剩余工作拆成正式计划：
  - `plans/article-backend-optimization-plan-2026-04-19.md`
- 本轮不是继续铺功能，而是严格补合同与持久化边界，重点落在：
  - live HTTP 合同
  - retry 失败分支
  - cleanup 持久化
  - active future 恢复安全
- 本轮新增并通过的后端合同包括：
  - live HTTP 已覆盖：
    - `/jobs/{job_id}/inspect`
    - `/jobs/{job_id}/retry`
    - `/uploads/{upload_id}/cleanup`
    - 缺失 `job/upload/artifact` 的关键错误映射
  - jobs 层已锁住：
    - 本地源文件缺失时 retry 失败
    - upload record 缺失时 retry 失败
    - upload 文件缺失时 retry 失败
    - 非法 `max_attempts / retry_delay_seconds / timeout_seconds` 不落库
    - active future 不会被误恢复成 stale failed
  - storage 层已锁住：
    - `job_cleanup` payload 可重载
    - `upload_cleanup` payload 可重载
    - repeated cleanup 的 `attempt_count` 连续递增
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `87 passed`
  - `/Users/apple/Desktop/article/.venv/bin/python -m pytest -q tests/test_article_http_smoke.py` -> `4 passed`
  - `python3 -m pytest -q` -> `390 passed, 1 xfailed`
- 当前后端状态判断：
  - article 后端仍然是“单机可用、合同稳定度明显提升的本地后端原型”
  - 但已比上一轮更适合让未来前端长期依赖
  - 下一步仍应优先继续 article 专属层，不把 article 需求下沉到 thesis 共享核心

### 第十一阶段：Storage 版本化骨架落地
- 已继续在 article 专属层推进后端，而没有下沉到 thesis 共享核心。
- 本轮新增：
  - `scripts/article_api/storage.py` 已加入最小 `schema version / migration` 骨架
  - 当前通过 `PRAGMA user_version` 维护 schema 版本
  - 支持：
    - 新库初始化写入当前 schema version
    - `user_version = 0` 的旧库自动迁移到当前版本
    - 遇到比当前实现更高的 schema version 时直接拒绝加载
- 已新增 storage 层测试：
  - schema version 初始化
  - legacy version `0` 迁移
  - newer schema version 拒绝加载
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_storage.py` -> `9 passed`
  - `python3 -m pytest -q tests/test_article_jobs.py tests/test_article_engine.py` -> `44 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `93 passed`
  - `python3 -m pytest -q` -> `396 passed, 1 xfailed`
- 当前判断：
  - storage 已从“只有表创建”进入“有版本号、有最小迁移入口”的阶段
  - 但 migration 仍只是最小骨架，尚未进入多版本演进、索引治理、备份恢复

### 第十二阶段：单 worker lease / heartbeat 元数据骨架验证
- 本轮继续只在 `scripts/article_api/jobs.py` 和 `tests/test_article_jobs.py` 内收口，没有把 article 运行时需求下沉到 thesis 共享主链。
- 已在 job runtime 元数据中固定最小观测字段：
  - `worker_model = single`
  - `lease_state = pending/active/released`
  - `last_heartbeat_at`
  - `heartbeat_count`
- 已补两条 jobs 层测试，分别锁定：
  - 运行中 job 的 `lease_state = active`
  - 完成后 job 的 `lease_state = released`
- 这批字段当前只用于单 worker 运行观测，不引入新的 `job.status`，也不改变 `summary.business_status` 语义。
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_jobs.py tests/test_article_engine.py` -> `48 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `94 passed`
  - `python3 -m pytest -q` -> `397 passed, 1 xfailed`

### 第十三阶段：Retention Automation 最小版落地
- 本轮继续只在 `article_api` 专属层推进，没有把 retention 需求下沉到 thesis 共享主链。
- 已新增最小运维接口：
  - `POST /ops/retention/sweep`
- 已实现的 retention 行为：
  - 按 age threshold 清理已完成 job 的 workspace / 产物
  - 按 age threshold 清理 upload registry 下的源文件
  - 复用既有 cleanup 审计结构，不重算 frozen `summary/result/runtime/artifacts`
  - cleanup payload 的 `policy` 会标记为 `retention`
  - 支持 `dry_run`
- 本轮新增测试覆盖：
  - jobs 层 dry-run retention 不落 cleanup
  - jobs 层 retention cleanup 后 frozen snapshot 仍稳定
  - fake app retention sweep
  - live HTTP retention sweep
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_jobs.py tests/test_article_api.py tests/test_article_http_smoke.py` -> `75 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `100 passed`
  - `/Users/apple/Desktop/article/.venv/bin/python -m pytest -q tests/test_article_http_smoke.py` -> `5 passed`
  - `python3 -m pytest -q` -> `403 passed, 1 xfailed`
- 额外工程经验：
  - 依赖 SQLite 状态根的 article 套件不能并行跑共享默认状态根；并行会产生假失败，必须串行或隔离 `ARTICLE_API_STATE_ROOT`

### 第十四阶段：Readiness / Version / Observability 最小版落地
- 本轮继续只在 `article_api` 专属层推进，没有把运维接口下沉到 thesis 共享主链。
- 已新增只读接口：
  - `GET /ready`
  - `GET /version`
  - `GET /ops/summary`
- 当前最小能力：
  - `ready` 校验 SQLite 状态根与 schema version 可读
  - `version` 返回 `service / stage / version / api_version`
  - `ops/summary` 返回 job lifecycle 计数、upload 可用性计数、state root 与 schema version
- 本轮新增测试覆盖：
  - fake app health/ready/version/summary 合同
  - readiness 失败映射 `503`
  - live HTTP `ready / version / ops summary`
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_http_smoke.py` -> `35 passed`
  - `/Users/apple/Desktop/article/.venv/bin/python -m pytest -q tests/test_article_http_smoke.py` -> `5 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `102 passed`
  - `python3 -m pytest -q` -> `405 passed, 1 xfailed`

### 第十五阶段：Recovery / Runtime 语义补强
- 本轮继续只在 `article_api.jobs` 和 `tests/test_article_storage.py` 内收口，没有把恢复语义下沉到 thesis 共享主链。
- 本轮修正的两个真实问题：
  - stale recovery 时，旧 runtime 缺字段会自动回补到当前 runtime 合同
  - stale apply 若 frozen result 仍在但 artifacts 丢失，会按 frozen result 重建最小 artifacts 语义，而不是直接退化成失败产物语义
- 本轮新增测试覆盖：
  - recovery 回补 runtime 合同字段
  - recovery 在缺失 artifacts 时重建 frozen output artifact
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_storage.py tests/test_article_jobs.py` -> `53 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py -x` -> `104 passed`
  - `python3 -m pytest -q` -> `407 passed, 1 xfailed`

### 第十六阶段：Retention 默认配置与机会式 Autorun
- 本轮继续只在 `article_api.app` 专属层推进，没有引入独立后台线程，也没有把调度语义下沉到 thesis 共享主链。
- 本轮新增：
  - `POST /ops/retention/run-defaults`
  - 基于环境变量的 retention 默认配置：
    - `ARTICLE_API_JOB_RETENTION_SECONDS`
    - `ARTICLE_API_UPLOAD_RETENTION_SECONDS`
    - `ARTICLE_API_RETENTION_AUTORUN`
    - `ARTICLE_API_RETENTION_AUTORUN_INTERVAL_SECONDS`
  - 机会式 autorun retention：
    - 在本地单机 API 的写操作入口按间隔触发
    - 不启独立线程
    - 最近一次 autorun / manual-defaults 结果会进入 `/ops/summary`
- 本轮新增测试覆盖：
  - fake app run-defaults 合同
  - fake app 缺省默认值报错
  - fake app autorun retention
  - live HTTP run-defaults
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_http_smoke.py` -> `39 passed`
  - `/Users/apple/Desktop/article/.venv/bin/python -m pytest -q tests/test_article_http_smoke.py` -> `6 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `108 passed`
  - `python3 -m pytest -q` -> `411 passed, 1 xfailed`

### 第十七阶段：Runtime Event Stream 与异常收口补稳
- 本轮继续只在 `article_api.jobs` 和 `tests/test_article_jobs.py` / `tests/test_article_storage.py` 内收口，没有把 article 运行时需求下沉到 thesis 共享主链。
- 已新增 job runtime 最小结构化事件流：
  - `job_queued`
  - `worker_started`
  - `attempt_started`
  - `attempt_failed`
  - `retry_scheduled`
  - `attempt_succeeded`
  - `job_failed`
  - `job_succeeded`
  - `recovered_as_failed`
- 这批事件当前只作为 runtime snapshot 的可读观测面，不扩张 `job.status`，也不引入新的 lifecycle 枚举。
- 本轮同时补上一个真实稳定性缺口：
  - 如果 worker 已成功返回，但后续在 summary / artifact 冻结阶段抛出异常，job 现在会立即落为 `failed`
  - 不再先写出“已结束但仍是 running”的短暂不一致状态，再依赖 stale recovery 事后兜底
- 本轮新增测试覆盖：
  - runtime event stream 顺序
  - stale recovery 的 `recovered_as_failed`
  - orchestration 异常即时失败收口
- 本轮再次确认：
  - 依赖 SQLite 状态根的 article 套件不要并行跑共享默认状态根
  - 并行假失败不计入正式基线，正式结论只认串行结果
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_jobs.py` -> `43 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `109 passed`
  - `python3 -m pytest -q` -> `412 passed, 1 xfailed`

### 第十八阶段：本地应用壳层启动
- 本轮继续只在 `article_api` 专属层推进，没有把本地软件运行壳需求下沉到 thesis 共享主链。
- 已新增本地应用壳层命令：
  - `article-local`
  - `article-api`
  - `article-doctor`
  - `article-backup`
  - `article-restore`
- 已新增 `ARTICLE_API_RUNTIME_ROOT` 环境变量覆盖，和 `ARTICLE_API_STATE_ROOT` 一起组成当前本地运行目录的显式配置入口。
- 已新增本机自检能力：
  - `article-doctor` 可直接输出 storage / runtime / retention 只读摘要
  - 适合不启动 HTTP 时做本机环境确认
- 已新增最小备份恢复闭环：
  - backup / restore 按“状态数据 / 运行产物”分层归档
  - 避免把 SQLite 状态层与 upload/job/runtime 产物混成一层
  - restore 默认要求目标目录为空；显式 `--force` 才覆盖现有根目录
- 本轮新增测试覆盖：
  - packaging metadata 新入口
  - doctor 根目录自检
  - backup / restore roundtrip
  - restore 非空目标拒绝覆盖
  - serve 命令的 uvicorn 启动参数与根目录覆盖
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_packaging_metadata.py tests/test_article_local_app.py tests/test_article_api.py tests/test_article_storage.py tests/test_article_uploads.py` -> `59 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `116 passed`
  - `python3 -m pytest -q` -> `418 passed, 1 xfailed`
- 当前判断：
  - article 后端已经足够继续往“本地应用软件的运行壳”推进
  - 但还不能算“后端已完整”
  - 下一步仍应优先收口：
    - 存储维护与多版本 migration
    - 更完整的 lease / heartbeat 恢复语义
    - 更细的结构化运维观测
    - 更正式的本地安装/升级交付脚本

### 第十九阶段：Storage Maintenance 与 Recovery Observability
- 本轮继续只在 `article_api` 专属层推进，没有把数据库、运行目录、worker 或 cleanup 语义下沉到 thesis 共享主链。
- 本轮新增 storage maintenance 能力：
  - `storage.py` 的 schema version 提升到 `2`
  - 已新增最小索引治理：
    - `idx_jobs_status_updated_at`
    - `idx_jobs_finished_at`
    - `idx_artifacts_job_id_position`
    - `idx_uploads_created_at`
  - `inspect_storage()` 现在会返回索引存在性
  - `maintain_storage()` 现在支持：
    - `ANALYZE`
    - 可选 `VACUUM`
    - db 大小变化摘要
    - 最新 storage 快照
- 本轮新增 local delivery shell 收口：
  - `article-maintain` 命令入口
  - `article-local maintain` 子命令
  - 维护结果会带根目录、动作列表和最新 storage 摘要
- 本轮新增 runtime recovery observability：
  - `runtime_snapshot()` 现在暴露：
    - recovery strategy
    - recovered failed job 计数与 job_id 列表
    - stale heartbeat 阈值
    - stale heartbeat job 计数与 job_id 列表
  - 这批字段当前只用于运行观测，不改变 lifecycle，也不改变 stale recovery 失败收口策略
- 本轮新增测试覆盖：
  - schema version `1 -> 2` 迁移
  - storage 索引存在性
  - storage maintenance 报告
  - local app maintenance 命令
  - runtime snapshot 的 recovered failed 计数
  - runtime snapshot 的 stale heartbeat 观测
  - fake app / live HTTP 对 recovery strategy 与 storage index count 的合同
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_storage.py tests/test_article_jobs.py tests/test_article_local_app.py tests/test_packaging_metadata.py tests/test_article_api.py tests/test_article_http_smoke.py` -> `105 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `121 passed`
  - `python3 -m pytest -q` -> `423 passed, 1 xfailed`
- 当前判断：
  - article 已从“只有运行原型”推进到“具备最小维护与恢复观测能力的本地后端壳”
  - 但 recovery 仍主要是“失败收口 + 冻结结果不漂移”
  - 下一步仍应继续收口：
    - 更正式的多版本 migration 链
    - single-worker 恢复窗口与更细的健康判定
    - 更完整的结构化日志与 runbook

### 第二十阶段：显式迁移链与恢复窗口补齐
- 本轮继续只在 `article_api` 专属层推进，没有把数据库、运行目录、worker、cleanup 或 lifecycle 语义下沉到 thesis 共享主链。
- 本轮把 storage migration 从“写最新版本号”补成显式链路：
  - `0 -> 1`：建表
  - `1 -> 2`：补索引
  - `inspect_storage()` 现在同时暴露 `supported_schema_versions`
- 本轮把 local shell 继续收口到“本机可运维”最小形态：
  - `article-local` 统一承接 `serve / doctor / backup / restore / maintain`
  - `article-maintain` / `article-local maintain` 现在会返回维护动作、db 大小变化和最新 storage 摘要
  - backup manifest 现在带 schema 元数据，restore 会拒绝恢复“高于当前支持版本”的备份
  - restore 现在会拒绝归档成员越界写出恢复根目录
- 本轮把 recovery observability 从“只看 stale failed”补到“可区分恢复窗口”：
  - 新增 `ARTICLE_API_JOB_RECOVERY_GRACE_SECONDS`
  - `_reconcile_incomplete_jobs()` 现在会对无 active future 的 unfinished job 先走 grace window，再决定是否收口为 `failed`
  - `runtime_snapshot()` 现在暴露：
    - `grace_seconds`
    - `pending_recovery_job_ids`
    - `pending_recovery_count`
    - `recovered_failed_job_ids`
    - `stale_heartbeat_job_ids`
- 本轮同时补强运维摘要：
  - `GET /ops/summary` 现在包含 storage index 计数、recovered failed 计数、pending recovery 计数、stale heartbeat 计数
  - `GET /ready` 继续要求 `runtime_root` 可写，避免只有 SQLite 可用但运行目录不可写的假 ready
- 本轮新增测试覆盖：
  - explicit migration chain `0 -> 1 -> 2`
  - restore 拒绝高 schema 备份
  - restore 拒绝路径逃逸归档成员
  - runtime snapshot 的 pending recovery / grace window 观测
  - fake app / live HTTP 对 storage index 计数与 recovery 字段的合同
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_storage.py tests/test_article_jobs.py tests/test_article_local_app.py tests/test_article_api.py tests/test_article_http_smoke.py tests/test_packaging_metadata.py` -> `109 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `125 passed`
  - `python3 -m pytest -q` -> `427 passed, 1 xfailed`
- 当前判断：
  - article 已达到“开发者可操作的本地后端壳 + 本机维护壳”阶段
  - 但离“普通用户可直接安装和稳定使用的本地软件”还差安装交付、运行日志、恢复 runbook、更多 migration 版本治理和最终入口层

### 第二十一阶段：本地交付闭环、doctor 收口与边界减压
- 本轮继续用 article 专属层推进，没有把本地软件交付语义下沉到 thesis shared 主链。
- 本轮把“安装/交付闭环”补到最小可交付状态：
  - `README.md` 现在明确：
    - `pip install -e '.[api]'`
    - `pip install -e '.[api,dev]'`
    - `article-local` 是统一入口
    - `article-api` / `article-doctor` / `article-maintain` / `article-backup` / `article-restore` 是等价别名
  - 现在还补了推荐顺序：
    - 先 doctor
    - 再 serve
    - 再 maintain / backup / restore
- 本轮把 local shell 的 doctor 输出收口成“本机运维摘要”：
  - `local_app.py` 现在不只回底层 payload，而是输出：
    - headline
    - issues
    - recommended actions
    - inventory
    - workflow 命令模板
  - `app.py` 现在把 ops summary / storage / runtime 摘要层显式结构化，doctor 直接复用这组摘要
- 本轮把 article adapter 的一个脆弱重复点收掉：
  - `article_engine/service.py` 不再自己复制一套规则动作判定
  - 现在改为复用 `thesis_tool/workflow.py` 暴露的共享 helper
  - 目标是让 article adapter 只负责结果整形，不再维护一套平行的 `C01 / KW01` 例外语义
- 本轮同时补了测试与运行隔离：
  - packaging metadata 现在会校验 console script target 可解析到真实 callable
  - `article-local --help` 和所有 alias `--help` 已补最小合同测试
  - local shell 的 root 覆盖现在通过 scoped env 恢复，避免 helper 调用污染后续测试
  - `tests/conftest.py` 现在默认给 article 测试隔离 `ARTICLE_API_STATE_ROOT` 和 `ARTICLE_API_RUNTIME_ROOT`
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_local_app.py tests/test_packaging_metadata.py tests/test_article_api.py tests/test_article_engine.py tests/test_scope_workflow.py` -> `78 passed`
  - `python3 -m pytest -q tests/test_article_local_app.py tests/test_article_jobs.py tests/test_article_api.py` -> `94 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `135 passed`
  - `python3 -m pytest -q` -> `437 passed, 1 xfailed`
- 当前判断：
  - article 已从“开发者可操作的本地后端壳”推进到“有安装入口说明、有运维自检摘要、有最小交付合同测试的本地 alpha 壳”
  - 下一步最值得继续收口的是：
    - 真正的安装/升级脚本或可分发包
    - 更正式的 runbook / 故障恢复手册
    - API 合同里对内部路径暴露的进一步收敛

### 第二十二阶段：本地初始化入口落地
- 本轮继续只改 article 专属入口层，目标是让第一次本地启动不再依赖手工拼目录和手工理解运行结构。
- 本轮新增 `article-local init`：
  - 初始化 SQLite 状态库
  - 预创建 `jobs / uploads / staging`
  - 可选写出环境变量文件
  - 直接返回下一步推荐命令：`doctor` / `serve`
- 本轮同步补了本地使用文档：
  - `README.md` 现在推荐流程已调整为 `init -> doctor -> serve -> maintain -> backup/restore`
  - 已补 `article-local init --help` 与 env 文件用法
- 本轮新增测试覆盖：
  - `init` 子命令出现在 `article-local --help`
  - `initialize_local_workspace()` 会创建 roots 和 env 文件
  - 已存在 env 文件时要求显式覆盖
  - `main(["init", ...])` 会输出 next steps
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `21 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `138 passed`
  - `python3 -m pytest -q` -> `440 passed, 1 xfailed`
- 当前判断：
  - article 已进一步从“开发者可操作 alpha 壳”推进到“普通本地用户第一次启动不再需要手工拼运行目录”的阶段
  - 下一步最值钱的仍是：
    - 安装/升级脚本或可分发包
    - API 对内部路径暴露的继续收敛
    - 面向故障恢复的正式 runbook

### 第二十三阶段：repo 级安装脚本落地
- 本轮继续朝“本地可使用软件”推进，但仍只改 article 本地入口层和 repo helper，不碰 thesis 共享主链。
- 本轮新增 repo 级安装脚本：
  - `python3 scripts/install_article_local.py`
  - 默认会：
    - 创建 `~/.article/venv`
    - 安装当前仓库的 `.[api]`
    - 初始化 `~/.article/state` / `~/.article/runtime`
    - 写出 `~/.article/article-local.env`
- 本轮同时保留一个稳定的校验路径：
  - `--no-deps --skip-init`
  - 用于无网络或只想验证安装链路时，避免默认执行依赖安装和初始化
- 本轮新增测试覆盖：
  - 安装脚本参数与命令构造合同
  - `--no-deps` 必须配合 `--skip-init`
  - 安装失败时会给出清晰错误，而不是直接把 pip traceback 当最终用户提示
  - `main()` JSON 输出合同
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_install_script.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `25 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py tests/test_article_install_script.py` -> `142 passed`
  - `python3 -m pytest -q` -> `444 passed, 1 xfailed`
- 当前判断：
  - article 已从“需要手工阅读 README 才能装起来”的 alpha 壳，推进到“仓库内已自带最小安装脚本 + 初始化入口 + 运维自检”的阶段
  - 下一步最值钱的仍是：
    - 真正的升级脚本或可分发包
    - API 内部路径暴露继续收敛
    - 正式故障恢复 runbook

### 第二十四阶段：本地安装链稳定性补强
- 本轮继续只改 article 本地安装/运维链，不下沉到 thesis 共享核心。
- 本轮补强点：
  - `scripts/install_article_local.py`
    - `--python` 现在既支持绝对路径，也支持 `PATH` 里的解释器名
    - `article-local init` 如果 stdout 不是合法 JSON，会收口成明确安装错误
  - `scripts/article_api/local_app.py`
    - env 文件输出现在做 shell-safe quoting
    - workflow 命令模板现在对 root 路径做 shell-safe quoting
    - env 文件若内容完全一致，重复执行 `init --write-env` 不再报错
    - 从已安装 venv 执行时，写出的 env 文件会补当前 `venv/bin` 到 `PATH`
    - `article-local serve` 结束后不再残留 root 环境变量
- 本轮新增测试覆盖：
  - 安装脚本 `--python` 解释器发现
  - 安装脚本 happy path 的 init 调用与 payload 解析
  - 安装脚本对非法 init stdout 的错误收口
  - local env 文件重复写入幂等性
  - env 文件 PATH 注入与 shell-safe quoting
  - doctor workflow 命令的 shell-safe quoting
  - `serve` 只在运行期间设置 roots，退出后恢复进程环境
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_install_script.py tests/test_article_local_app.py` -> `28 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_local_app.py tests/test_packaging_metadata.py tests/test_article_install_script.py` -> `148 passed`
  - `python3 -m pytest -q` -> `450 passed, 1 xfailed`
- 当前判断：
  - article 已从“能安装起来的 alpha 壳”进一步推进到“重复执行更稳、shell 环境更稳、命令复制粘贴更稳”的本地单机后端壳
  - 当前距离“本地可用软件”的剩余差距主要收敛为：
    - 非 editable 的正式安装/升级交付
    - 更完整的故障恢复与运维手册
    - 前端/UI 壳与更完整的人机交互

### 第二十五阶段：收紧脆弱 fallback，继续推进本地交付
- 本轮没有再扩 thesis 新规则，主要收口 article 软件化链里几处已经过时的“原型期兜底”。
- 本轮收口点：
  - `scripts/thesis_tool/workflow.py`
    - `plan / verify / diagnostics` 现在显式带出 `requested_profile` 与 `fallback_used`
  - `scripts/fix_thesis.py`
    - dry-run preview 现在也显式带出 `requested_profile` 与 `fallback_used`
  - `scripts/article_engine/service.py`
    - 不再通过 `profile_display` 里是否含有 `fallback:` 反推 fallback 状态，改为直接消费结构化字段
  - `scripts/install_article_local.py`
    - 安装结果现在直接返回 `quickstart` 命令
    - 有 env 文件时优先给 `article-local doctor/serve/...`
    - 跳过 init 时继续给 venv 内绝对路径命令，避免用户误以为 alias 已经可用
  - `tests/test_article_http_smoke.py`
    - HTTP smoke 继续走 `FastAPI TestClient` 主链，不再把 `uvicorn` 当成测试前置依赖
- 本轮工程判断：
  - thesis 主链当前是稳定维护期，今天最值得推进的是 article 的“使用成本”与“原型残留收口”
  - `profile_display` 应该只负责展示，不应继续承担状态反解
  - TestClient smoke 不依赖实际端口，更不应再额外挂 `uvicorn` 保护
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_engine.py tests/test_article_install_script.py tests/test_article_local_app.py` -> `36 passed`
  - `python3 -m pytest -q tests/test_article_http_smoke.py` -> `6 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py` -> `117 passed`
  - `python3 -m pytest -q tests/test_scope_workflow.py tests/test_workbench_cli.py` -> `34 passed`

### 第二十六阶段：补上非 editable 本地交付路径
- 本轮继续只推进 article 本地交付链，不改 thesis 共享规则。
- 本轮新增能力：
  - `scripts/install_article_local.py`
    - 新增 `--install-mode editable|wheel`
    - `wheel` 模式会先在 `--artifact-dir` 产出 wheel，再通过 direct URL requirement 安装进本地 venv
    - 安装结果 payload 现在显式带：
      - `install.mode`
      - `install.build_command`
      - `distribution.artifact_dir`
      - `distribution.wheel_path`
- 本轮工程判断：
  - 之前的安装壳已经够“开发者本地使用”，但还不够像“可以给别人直接装”
  - 先补 wheel 安装路径，比贸然做 GUI、桌面壳或更重的分发方式更划算
  - 当前默认仍保守保持 `editable`，避免打断既有开发流程；交付态明确走 `--install-mode wheel`
- 本轮同步文档：
  - `README.md` 已补 wheel 安装推荐入口和参数说明
- 本轮新增测试覆盖：
  - wheel 模式构建 artifact 并从 wheel direct URL 安装
  - 未知 install mode 拒绝执行
  - 原 editable 路径合同继续保持
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_install_script.py tests/test_article_local_app.py tests/test_packaging_metadata.py` -> `33 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_engine.py tests/test_article_http_smoke.py tests/test_article_install_script.py` -> `126 passed`

### 第二十七阶段：本地批量入口、多 profile 可见骨架与 wheel 回滚链
- 本轮继续只推进本地使用链路，不碰前端。
- 本轮新增能力：
  - `scripts/article_api/local_app.py`
    - 新增 `article-local profiles`
    - 新增 `article-local batch <audit|plan|verify|apply>`
    - batch 当前支持目录输入、`--recursive`、`--summary-file`、`apply --output-dir`
  - `scripts/thesis_workbench.py`
    - 新增 `profiles` 命令，直接列出可用 profile
  - `scripts/_profile_utils.py`
    - 新增 profile catalog，当前至少把 `cn-common`、`lnu-checker-2026` 显式暴露出来
  - `scripts/install_article_local.py`
    - 新增 wheel 安装历史
    - 新增 `--upgrade`
    - 新增 `--rollback`
- 本轮工程判断：
  - 本地高频体验里，batch 比继续补单文件壳更值钱
  - 学校 profile 先聚焦 `lnu` 主线，比现在就做大规模 school 扩张更稳
  - wheel 安装如果没有历史与回滚，就还不算真正可交付
- 本轮新增测试覆盖：
  - `article-local profiles` 输出
  - batch audit / apply 最小合同
  - `thesis_workbench.py profiles`
  - `audit_document(..., profile_path='lnu')`
  - wheel install history / rollback
- 本轮验证结果：
  - `python3 -m pytest -q tests/test_article_local_app.py tests/test_article_install_script.py tests/test_workbench_cli.py tests/test_article_engine.py` -> `60 passed`
  - `python3 -m pytest -q tests/test_profile_metadata_sync.py` -> `6 passed`
  - `python3 -m pytest -q tests/test_article_api.py tests/test_article_jobs.py tests/test_article_storage.py tests/test_article_uploads.py tests/test_article_http_smoke.py` -> `109 passed`
- 本轮补强：
  - `--upgrade` 现在会自动切到 wheel 链路，避免误落回 editable 安装
  - 新增 rollback 无历史时报错测试，避免本地交付时出现“命令存在但不可判定失败原因”的假阳性
  - 把 profile catalog 和 batch workflow 抽成 `article_api/profile_batch.py` 共用模块，减少 CLI / HTTP 双份实现
  - `article-api` 新增 `GET /profiles` 和 `POST /batch`，本地网页可以直接消费，不用再额外包一层 CLI
  - `article-api` 新增 `POST /jobs/batch`，批量任务现在也能进入统一 job 历史、结果查询和 retry 链
  - `article_api/jobs.py` / `article_api/job_runner.py` 已接入 batch operation；批量 summary JSON 也能作为 job artifact 下载
  - `GET /jobs` 现在支持 `operation/status/limit` 筛选，方便本地网页直接拉任务列表
  - `GET /jobs/batches/recent` 提供最近批量任务摘要视图，减少前端自己拼聚合逻辑
